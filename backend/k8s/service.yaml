apiVersion: v1
kind: Service
metadata:
  name: health-api-svc            # ← your desired service name
spec:
  selector:
    app: health-api               # ← MUST match pod label above
  ports:
    - port: 4000
      targetPort: 4000
  type: ClusterIP